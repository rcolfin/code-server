# code-server

This repo is installing Python dependencies on top of the [docker-code-server](https://github.com/linuxserver/docker-code-server) image.

See https://hub.docker.com/r/linuxserver/code-server for documentation
